{
  "name": "assets",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:js": "esbuild js/app.js --bundle --outfile=dist/app.js --minify",
    "build:css": "tailwindcss -i css/app.css -o dist/app.css --minify",
    "build": "npm run build:js && npm run build:css",
    "watch:js": "esbuild js/app.js --bundle --outfile=dist/app.js --watch",
    "watch:css": "tailwindcss -i css/app.css -o dist/app.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "daisyui": "^5.5.8",
    "esbuild": "^0.27.1",
    "tailwindcss": "^4.1.17"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.20",
    "@tailwindcss/cli": "^4.1.17"
  }
}
